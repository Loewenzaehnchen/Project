import tkinter as tk


root = tk.Tk()
root.title("Ecotourism")
root.minsize(500, 500)
root.maxsize(1500, 1500)
root.geometry("1300x800+50+50") #initial size and position of the window


tk.Label(root, text="Hello Traveller", font=("Comic Sans", 30), fg="White").pack()
tk.Label(root, text="Where do you want to go?", font=("Comic Sans", 30), fg="white").pack()
image = tk.PhotoImage(file="/Users/benjaminwright/Library/Mobile Documents/com~apple~CloudDocs/Art I Like/Oleg Riabchuk/Break of Spring.png")
tk.Label(root, image=image).pack()


from tkinter import ttk

root = tk.Tk()
root.title("Eco")
root.geometry("900x500")
root.configure(bg="gray")

label = tk.Label(root, text="Hello! Welcome to the Start Screen", font=("Comic Sans", 30), anchor="se", fg="dark green", bg="gray")
label.pack(expand=True)
def show_state():
    checkbox.config(text=f"Are you 18 or older? ({unchecked})")

var = tk.IntVar()
checkbox = tk.Checkbutton(root, text="Are you 18 or older?", variable=var, fg="dark green", bg="gray")
checkbox.select()
checkbox.config(command=show_state)
checkbox.pack(padx=5, pady=10)

def on_click():
    label.config(text="Welcome to Ecotourism!")
def open_second():
    root.destroy()
    second_window()
def second_window():
    win = tk.Tk()
    label = tk.Label(win, text="Second window")
    label.pack()

button = tk.Button(root, text="Click to Continue", command=open_second, fg="dark green", bg="Black", anchor="center")

button.pack(padx=5, pady=5)






# A helper label to show the result of the click
label = tk.Label(root, text="Waiting for click...", fg="dark green", background="gray")
label.pack(padx=5, pady=5)

root.mainloop()
